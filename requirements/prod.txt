# Production dependencies for AI Audio Listener

-r base.txt

# Production optimizations
gunicorn  # WSGI server for production

# Monitoring and logging (production-ready)
structlog  # Structured logging
sentry-sdk[fastapi]  # Error tracking (optional)

# Health checks and monitoring
# prometheus-client  # For Prometheus metrics (optional)

# Security (production)
# python-jose[cryptography]  # JWT tokens (if needed)
# passlib[bcrypt]  # Password hashing (if needed)

# Database (if needed for persistence)
# sqlalchemy
# alembic
# psycopg2-binary  # PostgreSQL adapter

# Caching (Redis is already included in base.txt)

# File storage (for audio files)
# boto3  # AWS S3 (if using cloud storage)
# azure-storage-blob  # Azure Blob Storage (alternative)

# Background tasks (if needed)
# celery
# flower  # Celery monitoring
