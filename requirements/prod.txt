# Production dependencies for AI Audio Listener

-r base.txt

# Production optimizations
gunicorn==21.2.0  # WSGI server for production

# Monitoring and logging (production-ready)
structlog==23.2.0  # Structured logging
sentry-sdk[fastapi]==1.38.0  # Error tracking (optional)

# Health checks and monitoring
# prometheus-client==0.19.0  # For Prometheus metrics (optional)

# Security (production)
# python-jose[cryptography]==3.3.0  # JWT tokens (if needed)
# passlib[bcrypt]==1.7.4  # Password hashing (if needed)

# Database (if needed for persistence)
# sqlalchemy==2.0.23
# alembic==1.13.1
# psycopg2-binary==2.9.9  # PostgreSQL adapter

# Caching (Redis is already included in base.txt)

# File storage (for audio files)
# boto3==1.34.19  # AWS S3 (if using cloud storage)
# azure-storage-blob==12.19.0  # Azure Blob Storage (alternative)

# Background tasks (if needed)
# celery==5.3.4
# flower==2.0.1  # Celery monitoring
